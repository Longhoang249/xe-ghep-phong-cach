# PUBLIC DATA REMEDIATION QUEUE

Task: `DATA-002`

Status: `REVIEW`

| Priority | URL/Surface | Current Claim | Verified Reality | Required Change | Risk |
|---|---|---|---|---|---|
| RESOLVED | MP-003 and MP-005 route hero and price table | Before SPRINT-002A, numeric prices rendered as bare amounts such as `250.000đ/người`. | All eight stored numeric values on these two pages are `VERIFIED_FROM`. | SPRINT-002A renders all four service values on each page with “Từ” and keeps the variability note next to the table. | Resolved in SPRINT-002A; scoped regression coverage required. |
| RESOLVED IN SPRINT-003A | MP-004 route hero and price table | Before SPRINT-003A, numeric prices rendered as bare amounts. An early SPRINT-003A candidate incorrectly inherited Hải Dương - Hải Phòng prices. | Cát Bi has its own Owner-verified starting prices: 300k/600k/750k/150k. | Corrected candidate build renders the Cát Bi route-level prices with “Từ” and keeps the variability note next to the table. | Awaiting final validation and deployment. |
| RESOLVED | MP-003 and MP-005 Offer schema | Before SPRINT-002A, `Offer.price` emitted a numeric value with ambiguous fixed-price semantics. | The amount is a minimum starting price; final fare is trip-specific. | SPRINT-002A keeps an Offer description that explicitly says “Giá từ” and removes numeric `price`/`priceCurrency` from these two service Offers. | Resolved in SPRINT-002A; semantic correctness takes priority over a numeric Offer. |
| RESOLVED IN SPRINT-003A | MP-004 Offer schema | Before SPRINT-003A, `Offer.price` remained numeric and could be interpreted as fixed. | Cát Bi has its own verified route-level starting prices; airport operating details remain unknown and omitted. | Corrected candidate build uses a descriptive Offer with category “Giá bắt đầu” and no numeric `price`/`priceCurrency`. | Awaiting final validation and deployment. |
| RESOLVED IN REM-001 | Booking success summary and persisted `estimated_price` | Before REM-001, output label was “Tham khảo”; registered/custom-route formulas could produce a numeric result without semantic metadata. | Stored Phase 1 values are starting prices; traceable registered-route fallbacks are estimates; UNKNOWN/custom routes have no authoritative numeric fare. | REM-001 separates `STARTING_FROM`, `ESTIMATE` and `CONTACT` in UI/payload, nulls unclassified numeric input server-side, and adds persisted semantic/unit fields. | Candidate is evidence-safe; database migration `003_add_booking_price_semantics.sql` must precede application deployment. |
| RESOLVED IN REM-001 | Booking cargo helper/formula | Before REM-001, public copy said price was calculated by distance and volumetric weight, and code applied that formula. | Parcel service and stored starting prices are verified, but no calculation or surcharge formula is verified. | REM-001 removes formula execution and replaces the helper with “Giá gửi hàng được kiểm tra theo thông tin hàng hóa và chuyến thực tế.” Cargo detail collection remains intact. | Candidate removes the unverified business rule without reducing lead detail. |
| COMPLIANT | Homepage quick-route tiles | Numeric shared price already renders as `Từ …`; missing values render “Liên hệ giá”. | Matches `VERIFIED_FROM` semantics for Phase 1 stored prices. | Preserve behavior; do not infer endpoint-specific prices. | Low. |
| REVIEW | Homepage cargo banner | “Chỉ từ 150k”. | Parcel service is verified; 150k is the lowest stored Phase 1 parcel starting price. Route/scope qualification is not explicit. | Keep out of this task; Strategy should decide whether generic scope is sufficiently clear. | Medium: scope ambiguity, not unsupported availability. |
| P1 | Route/guide parcel copy | Several assets state parcel service is accepted and add item/handling conditions. | Parcel availability is verified; item restrictions, handling rules, lead time and schedule remain unknown. | Preserve verified availability but remove/qualify unsupported operational details when separately approved. | Medium. |
| P1 | Quảng Ninh route/guide pages | Broad destination references. | Hạ Long/Bãi Cháy is now separately confirmed for MP-019; other named endpoints remain unconfirmed unless separately evidenced. | Keep generic corridor wording for unconfirmed endpoints; add endpoint claims only from Owner evidence. | Medium: service-area overclaim. |
| P1 | MP-005 scan-first endpoint orientation | The FAQ refers to “các endpoint được liệt kê”, but the SPRINT-005 scan-first renderer does not display the governed endpoint orientation/boundary block. | Endpoint names are geography/search orientation only; Hạ Long/Bãi Cháy has separate service evidence, while other named endpoints remain unconfirmed. | In a separately approved production task, either render the governed orientation/boundary block or revise the FAQ reference without adding availability/price claims. | Medium: confusing public copy and two failing SEO checks; not permission to create endpoint URLs. |
| P1 | Cát Bi pages | Existing airport instructions imply operational coverage. | Passenger route-level service and prices are verified; airport exceptions are not. | Obtain and then publish waiting, delay, early/late-flight, luggage, and surcharge rules only if needed. | Medium: operational mismatch. |
| P1 | All public surfaces | Some copy can imply immediate/daily availability. | “Both directions/full corridor” is verified; frequency is not. | Avoid “hàng ngày”, fixed timetable, or guaranteed vehicle until verified. | Medium: availability overclaim. |
| P1 | Legacy price fallbacks PF-001…PF-008 | Formula-derived values can be produced. | Traceable `ESTIMATE`, `DATA_REQUIRED`, forbidden for new assets. | Replace one by one with Owner-verified numeric/range/from/variable rules. | High: formula mistaken for business truth. |

REM-001 resolves the two booking/cargo P0 rows only. Homepage cargo-banner scope, route/guide parcel wording, endpoint boundaries, airport operations, frequency claims and legacy data replacement remain separate remediation work.
